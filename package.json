{
  "name": "rsp",
  "private": true,
  "version": "0.0.1",
  "description": "Respectful Synchronised Protocol — monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
